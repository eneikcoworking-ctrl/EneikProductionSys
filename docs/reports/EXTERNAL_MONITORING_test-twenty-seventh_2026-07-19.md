# Внешний независимый мониторинг — test-twenty-seventh (первый эксперимент после ночного цикла фиксов)

**Роль:** внешний строгий аудитор, независимый от внутреннего мониторинга системы.

**Проект:** `test-twenty-seventh`, id `591b647f-a467-4a74-a881-57e00d54b642`
**Создан:** 2026-07-19T02:33:26Z — **первый проект, созданный через новый обязательный флоу "вишлист при создании"** (backend отклоняет создание без `initialWishlist`; фронтенд теперь показывает модальное окно перед созданием).
**Правило:** только чтение, ничего в системе не менять — не запускать docker build/up, не менять настройки, не писать в БД/API — только GET-запросы и `docker compose logs`.

## Контекст: что изменилось со времени test-twenty-sixth (в порядке деплоя этой ночью)

1. `ClaimService.detectStuckSessions` теперь покрывает `revising`-сессии, не только `running` — застрявшие после отклонения ревью сессии больше не невидимы для восстановления.
2. Design-review концерн на уже одобренном дизайне больше не порождает новый Stitch-мокап и новый цикл ревью — правка идёт прямо в задачу фронтенда со ссылкой на существующий approved-мокап.
3. Декомпозиция теперь явно требует, чтобы каждый пункт заканчивался закоммиченным файлом (код/текст/цены/дизайн-как-код) — не просто решением без артефакта.
4. Record-PR (план компилятора, вердикты ревью/дизайна, отчёт фальсификации) теперь **мерджатся** и архивируются под уникальным именем в `.eneik/records/`, а не просто закрываются без мерджа.
5. **Обязательный вишлист при создании проекта** — backend отклоняет пустой проект, фронтенд показывает модалку перед созданием.
6. Rate-limit fix: `reconcileAbandonedPullRequests` — окно сокращено с 7 дней до 3 часов + cooldown 10 минут между проверками одной сессии (был вероятный главный источник исчерпания GitHub API лимита).
7. `EmsMetricsService`: системные/мета-задачи (компилятор, фальсификация, PR-review-fallback, design-review) исключены из расчёта Role Doctrine Readiness — не засчитываются как выполнение доктрины роли.

Дополнительно: этой ночью вручную заблокированы (status=blocked) 253 зависших задачи по всем неактивным проектам; подтверждено, что сейчас 0 проектов в статусе `active` кроме этого нового.

## Метод

Каждая запись:
- `GET /api/projects/591b647f-a467-4a74-a881-57e00d54b642/dashboard`, `/command-dashboard`, `/pull-requests`, `/recent-activity?limit=60`
- `GET /api/system-status?projectId=591b647f-a467-4a74-a881-57e00d54b642`
- `docker compose logs backend --since <окно>` при необходимости деталей

В фокусе в этом прогоне:
1. Подтверждается ли живьём каждый из 7 фиксов выше.
2. Не создаётся ли снова каскад "design concern → новый мокап → новое ревью → новый concern" (fix #2).
3. Мерджатся ли record-PR по-настоящему, появляются ли файлы в `.eneik/records/` (fix #4).
4. Не возвращается ли GitHub rate-limit (fix #6).
5. Отражают ли метрики роли реальность, а не шаблон (fix #7).
6. Стандартные проверки: дубли/мусор в названиях, честность Lean/TOC/Six Sigma полей, зависшие сессии, SYSTEM STALLED.

---

## T+0 — Baseline (2026-07-19T02:35:17Z)

Проект создан 2 минуты назад через новый обязательный флоу. **Задачи: 2, wishlist: 2** — сразу же bootstrap-задача (`9881a1eb`, source=role) плюс клиентское ТЗ (`83ea27a1`, source=client, 2441 символ, статус `compiling` — уже подхвачено компилятором). Это ожидаемо и корректно: bootstrap теперь создаётся сразу, потому что вишлист **уже есть** с момента создания (Lean-фикс #6 из прошлой ночи был про "не создавать bootstrap, если вишлиста ВООБЩЕ нет" — сейчас он есть с первой секунды, так что немедленное появление bootstrap — не регрессия).

Далее буду вести обычные периодические записи по установленному ранее формату.

## T+30 — 2026-07-19T03:05:10Z

Сильная отметка — большинство фиксов подтвердились живьём одновременно.

**Фикс #2 (design-cascade) — подтверждён напрямую.** Задача "Design Brief" (`8c01a17e`, роль BARCAN-TAG-03) выросла из concern'а на `design/approved/20260719024424-mockup` (снова находка про touch target/закон Фиттса — на новом проекте, значит ревьюер реально анализирует заново, а не повторяет готовый ответ). В её описании прямым текстом: *"DESIGN_MOCKUP_ASSET (already approved - this is a correction to existing design, not new UI; implement directly against it, no new mockup or design review needed): design/approved/.../mockup.html"*. Именно так и должно работать после вчерашнего фикса — ни новый Stitch-мокап, ни новый цикл ревью не запущены.

**Фикс #4 (merge-and-archive record PR) — подтверждён напрямую.** Все 3 closed PR в этом проекте — `merged: true` (#1 план компилятора, #3 вердикт design review, #4 decompose design brief). Логи подтверждают: *"Merged record PR .../pull/3 ... reason=design review verdict parsed"* и аналогично для #4. Ни одного "Could not archive" предупреждения — архивирование в `.eneik/records/` прошло тихо и без ошибок.

**Фикс #6 (GitHub rate-limit) — пока держится.** `/pull-requests` — `available:true`, без 403 за всё окно наблюдения. `reconcileAbandonedPullRequests`-related warning'ов не встретил.

**Фикс #7 (честные метрики роли) — подтверждён напрямую, и это именно то, что оператор просил вчера вечером на самом первом снимке.** `roleDoctrineReadiness`: 9 из 12 ролей — `unknown` (базовый балл 25, нет реальных задач), только 3 — `objects` (BARCAN-TAG-01, 03, и ещё одна — те, у кого реально есть owner-задачи: Runtime Contract, Design Brief, UI Slice). **0 ролей "satisfied" на этом раннем этапе — ровно то, что должно быть**, в отличие от вчерашнего шаблонного накручивания через BARCAN-TAG-09.

**Разблокировка аккаунтов держится.** `fivedmitr-sys` теперь `busy` — реально открыл PR #5 ("Design: Increase top app bar touch targets") под своим именем, то есть разблокировка была не просто косметической, аккаунт реально работает. `apiBlocked: 0`, новых заблокированных аккаунтов нет.

**Рост:** 2→7 задач, 2→5 wishlist за 30 минут — управляемо, объясняется по каждой единице (compile×2, design review, PR-review-fallback, плюс 3 реальных product-задачи).

**Lean/Six Sigma поля** остаются содержательными, не шаблонными: "100% of top app bar icon buttons have >= 44x44px touch target" (Design Brief), "reduction in support inquiries regarding Gemini API billing" (UI Slice, привязано к реальному содержанию ТЗ про ошибку биллинга Gemini API).

**Ошибок нет:** `DUPLICATE TASK TITLES` — 0, `SYSTEM STALLED` — 0, `VERIFICATION_SERVICE_UNAVAILABLE` — 0 упоминаний за 16 минут, непредвиденных ERROR — 0. `systemHealth.status: ok`, `minutesSinceProgress: 2`.

**Не проверено ещё:** каскад design-review (пункт а чек-листа) сработал только один раз здесь — недостаточно данных, чтобы утверждать, что цикл НЕ зациклится снова при повторных concern'ах на одном и том же элементе; продолжу следить. `detectStuckSessions` на `revising` (пункт д) — пока ни одна сессия не отклонялась повторно в этом эксперименте, случая проверить не было.

---

## T+45 — 2026-07-19T03:23:09Z

**Лучший результат за обе ночи: реальный код смёрджен, ноль concern'ов, каскад НЕ продолжился.** Задача "Design Brief" (`8c01a17e`) — та самая с фикса touch target — прошла полный цикл: PR #5 → PR-review-fallback → *"approved by Jules reviewer with 0 concern(s)"* → `AutoMergeService` смёрджил (23:08:09Z UTC-времени лога) → задача помечена `done`. **Ноль concern'ов** — предыдущие похожие находки на этом и прошлом проекте всегда давали 1-2 concern'а; здесь реализация оказалась чистой, и, что важно для проверки пункта (а) чек-листа — **новый wishlist/задача НЕ появились**. Wishlist остался на 5, задачи на 7 — каскад в этот раз не пошёл, потому что действительно нечему было продолжаться (не потому что механизм подавления сам сработал — тут просто честный чистый результат).

**RoleAdviceLoopService снова корректно промолчал** вместо фабрикации: *"Gemini unavailable for task 8c01a17e..., skipping advice generation this cycle (not fabricating a fallback)"* — фикс с прошлой ночи держится.

**detectStuckSessions активно работает по всей системе** — 6 разных сессий помечены *"is stuck (no real progress for 30 minutes)"* за это окно (`da571061`, `7bd02f10`, `fbb23a94`, `069dca1d`, `cba82e2c`, `13e9f3e7`). Все проверенные — статус `running`, не `revising` — то есть это подтверждает старую (уже работавшую) часть механизма, а не конкретно вчерашнее расширение на `revising`. Ни одна сессия в этом эксперименте пока не уходила в `revising`, так что пункт (б) чек-листа остаётся неподтверждённым для test-twenty-seventh конкретно — буду ждать случая.

**Единственные ERROR в логах — ожидаемые, не новая проблема:** `MLPredictionServiceClient: ML service chat call failed: 502... RESOURCE_EXHAUSTED` — это тот же исчерпанный Gemini prepaid-баланс, известный с самого начала ночи, correctly перехвачен RoleAdviceLoopService (см. выше), не маскируется под успех.

**Аккаунты стабильны:** idle:3, busy:2, apiBlocked:0 — разблокировка `fivedmitr-sys` не откатилась, новых `api_blocked` нет.

**Rate-limit:** `/pull-requests` `available:true`, 0 упоминаний 403 за окно — фикс #6 продолжает держаться.

**Дублей/мусора нет:** `DUPLICATE TASK TITLES` — 0, `SYSTEM STALLED` — 0.

---

## T+60 — 2026-07-19T03:40:10Z

**Наконец появился живой случай для последнего непроверенного пункта.** В 03:25:38Z задача `73bac114` ("UI Slice") получила отказ ревью и ушла в `revising` — *"Review rejected. Transitioning session sessions/2847834506897108526 to revising for task 73bac114..."*. С тех пор (уже ~15 минут) — тишина, ни новой попытки, ни повторного `pr_opened`. Это ровно та ситуация, которую вчерашний фикс должен ловить: `detectStuckSessions` теперь проверяет `revising`, но порог — 30 минут простоя. Сейчас рано делать вывод — жду следующую отметку (~03:55Z), когда порог будет пройден, и проверю, появится ли `"Maintenance: Session ... is stuck"` именно для этой сессии. Зафиксировал точное время старта, чтобы не потерять нить.

**Задачи/wishlist без изменений: 7/5** — 14 минут без прогресса (`systemHealth.minutesSinceProgress: 14`, `status: ok`, порог stall ещё далеко). Аккаунты сместились в сторону простоя: idle:4, busy:1 (только `eneikdru`) — ожидаемо, раз одна из рабочих сессий встала в revising, а остальные, видимо, между циклами.

**Record-PR продолжают мерджиться корректно** — без изменений с прошлой отметки, все 5 closed PR всё ещё `merged:true`.

**Ошибок нет** (кроме уже известного исчерпанного Gemini, не логировался в этом окне вообще). `DUPLICATE TASK TITLES` — 0, `SYSTEM STALLED` — 0. `apiBlocked` всё ещё 0 — разблокировка держится.

**detectStuckSessions продолжает ловить старые системные сессии** (`069dca1d` и другие, не из этого проекта) каждые ~5-11 минут — сам механизм явно жив и активен, вопрос только в том, поймает ли он именно `revising`-сессию `73bac114` конкретно.

---

## T+85 — 2026-07-19T04:03:00Z

**Перерыв в наблюдении: с T+60 (03:40Z) сессия мониторинга прервалась с ошибкой; возобновляю с этой отметки.** Backend успел сделать чистый рестарт (`Starting EneikProductionApplication` в 03:59:21Z → `Started` в 04:00:15Z), в логах вокруг рестарта нет ERROR/Exception/OOM — похоже на штатный перезапуск контейнера, не на падение из-за самого проекта. `system-status` сейчас отвечает нормально (`systemHealth.status: ok`, `minutesSinceProgress: 0`).

**Пункт (б) чек-листа (detectStuckSessions на `revising`) — сессия `73bac114` вышла из revising сама, раньше 30-минутного порога.** Сессия `sessions/2847834506897108526` (задача `73bac114`, "UI Slice" по Gemini-биллинг гайду), ушедшая в `revising` в 03:25:38Z, реально открыла новый PR **#7** ("feat(frontend): implement Gemini API prepayment troubleshooting guide and interactive simulator", автор `fivedmitr-sys`, ещё не смёрджен) — то есть работа не встала намертво. Но раз она ожила раньше порога, поймать её `detectStuckSessions`-проверкой на `revising` было не на чем — пункт остаётся формально непроверенным в этом эксперименте.

**Новая находка (не из исходного чек-листа): та же сессия сейчас "слепа" для backend, и статус задачи в системе разошёлся с реальностью на GitHub.** С 04:01:02Z по 04:02:50Z (три подряд минутных цикла) backend логирует: *"Jules activities response for session sessions/2847834506897108526 exceeded 2097152 bytes; skipping activity scan"* и *"...session left running, blind cycle 3/4/5"*. Из-за этого задача `73bac114` в БД всё ещё `status: claimed` — хотя PR #7 на GitHub уже реально открыт. Это тот же класс проблемы, что чинил вчерашний коммит `4490e23` ("recover Jules sessions stuck behind an oversized activity log") — но конкретно на этой сессии восстановление, судя по логам, не срабатывает: backend просто продолжает пропускать цикл за циклом, счётчик растёт (3→4→5), отката/recovery не видно. Дашборд по этой задаче сейчас нельзя считать источником правды — данные с GitHub (PR #7 open) на 30+ минут опережают то, что знает backend.

**Остальное без изменений или в норме.** Record-PR: все 5 закрытых PR (#1, #3, #4, #5, #6) — `merged:true`, включая новый #6 (PR-review-fallback verdict для #5) с момента T+60. Два PR ещё открыты: #7 (см. выше) и #2 ("Define Runtime Contract and Service Boundaries", `sixdmitrsix-ops`, бутстрап-задача `9881a1eb`) — по нему отдельных аномалий в логах не встретил. Rate-limit (фикс #6): `available:true`, 0 упоминаний 403. Аккаунты: `apiBlocked:0`, `busy:1` (`eneikdru`, PR-review-fallback), новых блокировок нет. `DUPLICATE TASK TITLES` — 0, `SYSTEM STALLED` — 0. Задачи/wishlist: pipeline `claimed:6, done:1, failed:0`, wishlist без изменений с T+60.

---

<!-- Следующие записи добавляются здесь по одной, в хронологическом порядке. Формат:
## T+X — HH:MMZ
[аналогичная структура]
-->
