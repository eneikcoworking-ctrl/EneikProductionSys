# Общие programming patterns аналитической философии

Этот файл содержит практики, которые подходят более чем пяти философам из Barcan-корпуса. Они вынесены сюда, чтобы персональные файлы философов не повторяли одно и то же и сохраняли индивидуальность.

| ID | Общий паттерн | Почему снижает дефекты | Правило использования |
|---|---|---|---|
| `ACP-001` | Design by Contract | Предусловия, постусловия и инварианты явно фиксируют обязанность кода, поэтому дефект ловится до скрытого распространения состояния. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-002` | Type-Driven Design | Типовая модель запрещает целые классы невозможных состояний ещё до запуска программы. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-003` | Property-Based Testing | Генеративная проверка ищет контрпримеры шире ручных примеров и особенно полезна для инвариантов. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-004` | Invariant-Centered Modeling | Система проектируется вокруг утверждений, которые всегда должны оставаться истинными. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-005` | Static Analysis Gate | Линтеры, type-check, SAST и schema-check переносят обнаружение дефекта в раннюю стадию кодинга. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-006` | Exhaustive Case Analysis | Все варианты состояния перечисляются явно; невозможное состояние не попадает в runtime. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-007` | Immutability by Default | Неизменяемые значения снижают риск скрытой мутации и гонок состояния. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-008` | Pure Function Core | Чистая функция делает зависимость входа и выхода проверяемой, локализуя эффекты на границах. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-009` | Boundary Validation | Все внешние входы валидируются на границе, а не внутри случайной бизнес-логики. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-010` | Schema and Contract Validation | Машиночитаемая схема делает договорённость проверяемой автоматически. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-011` | Idempotency | Повтор команды не создаёт вторичный дефект, дубликат или повреждение состояния. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-012` | Deterministic State Machine | Явные состояния и переходы исключают хаотичные промежуточные режимы. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-013` | Observability and Traceability | Логи, метрики, трассы и audit trail превращают подозрение в проверяемый факт. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-014` | Versioned API Contract | Изменение публичной формы поведения проходит через версию, а не через тихий дрейф. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-015` | Safe Rollback and Feature Flag | Изменение можно включить, измерить и откатить без разрушения системы. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-016` | Circuit Breaker and Bulkhead | Сбой ограничивается границами, не превращаясь в системную аварию. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-017` | Dependency Inversion | Высокоуровневая политика не зависит от низкоуровневых деталей, что снижает хрупкость изменений. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-018` | Anti-Corruption Layer | Наследие и внешние модели переводятся на границе, не заражая внутренний язык системы. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-019` | Data Lineage | Происхождение данных фиксируется, чтобы доверие к результату было проверяемым. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-020` | Least Privilege | Субъект получает только минимальные права, нужные для конкретного действия. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-021` | Zero Trust Verification | Ни один запрос не считается доброкачественным без проверки контекста, подписи и полномочий. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-022` | CI Quality Gate | Код не полагается на обещание разработчика: проверка встроена в pipeline. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-023` | Golden Master Regression | Наблюдаемое эталонное поведение сохраняется и защищается от случайной регрессии. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-024` | Transactional Outbox | Состояние и событие публикуются согласованно, без расщепления факта между БД и брокером. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-025` | Migration Rollback Plan | Любое изменение схемы или инфраструктуры имеет путь назад до применения в production. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-026` | Accessibility by Default | Доступность проектируется как базовый инвариант, а не поздняя косметика. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-027` | Single Writer Ownership | У каждого изменяемого участка есть владелец, поэтому два агента не принимают несовместимые решения в одном месте. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-028` | Contract-First Parallelism | Сначала фиксируется контракт, затем команды работают параллельно за стабильной границей. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-029` | Merge Queue Gate | Изменения попадают в main только через очередь, которая пересобирает результат на актуальной базе. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-030` | Generated Artifact Authority | Сгенерированные файлы меняются только генератором, поэтому ручные правки не конфликтуют с регенерацией. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-031` | Append-Only Extension Point | Новые возможности добавляются через новые файлы или расширения, а общий реестр собирается автоматически. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-032` | Refactor Freeze Lane | Массовый рефакторинг идёт в отдельном окне или ветке, не смешиваясь с продуктовой разработкой. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-033` | Conflict Forecast in PR | PR заранее объявляет зоны пересечения, владельцев и потенциальные конфликтные поверхности. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-034` | Semantic Conflict Test | Проверяется не только текстовый merge, но и смысловая совместимость контрактов, схем и поведения. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-035` | Migration Serialization | Миграции БД проходят через единый порядок, исключая две конкурирующие правки одной схемы. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-036` | Feature Flag Isolation | Незавершённая параллельная работа изолируется флагом и не меняет общий runtime без включения. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-037` | No Shared Constants Drift | Общие константы, enum и типы меняются только через владельца и impact-check. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |
| `ACP-038` | Conflict Resolution Evidence | Любое ручное разрешение конфликта подтверждается тестом или проверкой затронутого контракта. | Подключать как общий фон RAG; не дублировать в персональных списках философов. |

## Правило RAG

1. Сначала выбирай персональный файл философа по `barcan_tag` и имени.
2. Затем добавляй релевантные общие паттерны из этого файла как фон.
3. Не записывай общий паттерн повторно в персональный список философа.
4. Если новый паттерн начинает подходить более чем пяти философам, перенеси его сюда и замени в персональных файлах на индивидуальные микропаттерны.
