import os

ROLE_LOGIC = {
    "BARCAN-TAG-00_CODE-GUARDIAN.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Эпистемическая и прагматическая модальная логика (верификационистская модель верования и знания). Позволяет формализовать когнитивную уверенность в качестве и ясности кода на основе верифицируемого соответствия правилам.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $p$ — рассматриваемый Pull Request.
   Определим предикаты:
   * $C(p)$ — код в $p$ полностью соответствует единым синтаксическим и семантическим стандартам команды.
   * $T(p)$ — каждый новый публичный метод в $p$ изолированно покрыт проверяемым юнит-тестом.
   * $D(p)$ — декларативное описание (комментарии, описание PR) полностью совпадает с процедурным поведением кода.
   * $V(p)$ — уровень вложенности управляющих конструкций в $p$ не превышает 3, а длина функций $\le 40$ строк.
   * $M(p)$ — в коде отсутствуют «магические» константы и невынесенные литералы.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(p) \iff \Box (C(p) \land T(p) \land D(p) \land V(p) \land M(p))$$
     Роль обязана согласовать PR только тогда, когда качество, верифицируемость, отсутствие магических сущностей и когнитивная простота кода выполняются с абсолютной необходимостью.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(p) \iff \lozenge (\neg C(p) \lor \neg T(p) \lor \neg D(p) \lor \neg V(p) \lor \neg M(p))$$
     Роль обязана отклонить и заблокировать PR при малейшей возможности нарушения хотя бы одного из обязательных стандартов качества или верифицируемости.

3. **Функция Ценности (Value Function) и веса метрик:**
   Профиль роли ориентирован на максимизацию качества архитектуры и минимизацию сложности техдолга, сохраняя сбалансированное время ревью.
   $$Evaluation = 0.15 \cdot T + 0.50 \cdot Q + 0.10 \cdot V - 0.25 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.15 — быстрое проведение ревью критически важно для предотвращения блокировок в TOC-потоке.
   * $w_2$ (Качество $Q$) = 0.50 — максимальный приоритет отдаётся строгости кода, чистоте стиля и отсутствию скрытых дефектов.
   * $w_3$ (Ценность $V$) = 0.10 — бизнес-полезность фичи важна, но не может быть куплена ценой разрушения стандартов.
   * $w_4$ (Сложность $C$) = 0.25 — удержание низкого уровня сложности и предотвращение техдолга являются непосредственной зоной ответственности роли.

4. **Ключевая проектная метрика (KPI роли):**
   *Индекс семантического качества и соответствия стандартам (Code Compliance Index / CCI)* — процент Pull Requests, принятых в основную ветку без единого нарушения жестких синтаксических правил (Obligatory) с первой или второй итерации ревью.
""",
    "BARCAN-TAG-01_ACTUALIST-OBJECT.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Квантифицированная актуалистская модальная логика (логика Рут Баркан Маркус с актуалистской семантикой существования). Исключает оперирование потенциальными или гипотетическими сущностями, допуская существование только реально функционирующих доменных объектов.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $x$ — проектируемый архитектурный объект (сущность, модуль).
   Определим предикаты:
   * $E(x)$ — объект $x$ реально (актуально) существует в базе данных и бизнес-процессах системы (не является абстрактной заглушкой «на будущее»).
   * $An(x)$ — объект $x$ обладает анемичной моделью (содержит только данные без инкапсулированного доменного поведения).
   * $I(x, y)$ — нарушена физическая изоляция границ баз данных и контекстов между объектами $x$ и $y$ из разных доменов.
   * $C4(x)$ — архитектура объекта $x$ полностью отражена на актуальной диаграмме C4 и Bounded Context Map.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(x) \iff \forall x (E(x) \land \neg An(x) \land C4(x)) \land \Box (\forall y \neg I(x, y))$$
     Роль одобряет архитектурное решение тогда и только тогда, когда все новые сущности актуально обоснованы, не являются анемичными, задокументированы в C4, и гарантирована абсолютная необходимость изоляции доменных границ.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(x) \iff \exists x (\neg E(x) \lor An(x) \lor \neg C4(x)) \lor \lozenge (\exists y I(x, y))$$
     Роль блокирует решение, если обнаружено существование гипотетической заглушки, анемичность модели, отсутствие документации C4, или допускается возможность протекания бизнес-логики и нарушения границ контекста.

3. **Функция Ценности (Value Function) и веса метрик:**
   Роль нацелена на удержание строгой системной структуры и минимизацию сложности при высоком качестве декомпозиции.
   $$Evaluation = 0.10 \cdot T + 0.45 \cdot Q + 0.15 \cdot V - 0.30 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.10 — проектирование фундаментальных онтологий требует основательности и не терпит спешки.
   * $w_2$ (Качество $Q$) = 0.45 — безупречная чистота доменных моделей и строгость декомпозиции сервисов.
   * $w_3$ (Ценность $V$) = 0.15 — бизнес-ценность формируется через надлежащее структурирование реальных процессов.
   * $w_4$ (Сложность $C$) = 0.30 — высокий штраф за внесение избыточной сложности, дублирование сущностей и нарушение Anti-Corruption Layer.

4. **Ключевая проектная метрика (KPI роли):**
   *Индекс онтологической чистоты домена (Domain Purity Index / DPI)* — отношение количества неанемичных доменных сущностей (содержащих внутреннюю бизнес-логику и инварианты) к общему числу сущностей в системном реестре (целевое значение: 100%).
""",
    "BARCAN-TAG-02_RIGID-DESIGNATOR.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Двумерная модальная логика (модель Крипке для жесткой десигнации и двумерная семантика Чалмерса/Сталнакера). Обеспечивает неизменность значения идентификаторов во всех возможных мирах (окружениях выполнения) при гибком изменении их внутреннего прагматического контекста.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $c$ — интеграционный API-контракт, $n$ — идентификатор (имя эндпоинта или поля).
   Определим предикаты:
   * $R(n)$ — имя $n$ является жестким десигнатором (зафиксировано в Глоссарии и несёт строго одно значение во всей системе).
   * $B(c)$ — контракт $c$ обеспечивает полную обратную совместимость (Backward-compatible) для всех потребителей.
   * $T(c)$ — контракт $c$ полностью покрыт автоматическими контрактными тестами (Pact).
   * $E(c)$ — в контракте $c$ обработаны и специфицированы все потенциальные ошибочные состояния.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(c) \iff \Box (R(n) \land B(c) \land T(c) \land E(c))$$
     Роль одобряет контракт только при жесткой необходимости выполнения однозначности нейминга, обратной совместимости, полноты обработки ошибок и наличия автоматических тестов.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(c) \iff \lozenge (\neg R(n) \lor \neg B(c) \lor \neg T(c) \lor \neg E(c))$$
     Роль обязана заблокировать изменения, если существует малейшая возможность двусмысленности именования, нарушения обратной совместимости, пропуска тестов или неполноты обработки исключений.

3. **Функция Ценности (Value Function) и веса метрик:**
   Роль балансирует между стабильностью интерфейсов и интеграционной связностью.
   $$Evaluation = 0.15 \cdot T + 0.45 \cdot Q + 0.15 \cdot V - 0.25 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.15 — быстрая публикация спецификаций (Contract-First) ускоряет параллельную разработку.
   * $w_2$ (Качество $Q$) = 0.45 — бескомпромиссная надежность контрактов, отсутствие дублирования и омонимии.
   * $w_3$ (Ценность $V$) = 0.15 — ценность определяется бесшовностью интеграций внешних и внутренних клиентов.
   * $w_4$ (Сложность $C$) = 0.25 — штраф за внесение хрупких зависимостей и усложнение логики трансляции данных.

4. **Ключевая проектная метрика (KPI роли):**
   *Коэффициент стабильности контрактов (Contract Compatibility Index / CCI)* — доля публичных API-интерфейсов, изменившихся без нарушения обратной совместимости и без необходимости экстренной остановки потребителей (целевое значение: 100%).
""",
    "BARCAN-TAG-03_BELIEF-INTENSION.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Интенсиональная и эпистемическая логика восприятия (моделирование феноменального опыта пользователя). Направлена на снижение когнитивного шума и удержание фокуса внимания через управление визуальными интенциями и пространственными законами.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $u$ — визуальный интерфейс (макет).
   Определим предикаты:
   * $S(u)$ — представлены макеты для двух обязательных разрешений (1440px и 375px).
   * $R(u)$ — макеты адаптивны, файлы не идентичны по байтам и отражают перекомпоновку под тач-интерфейс.
   * $Q(u)$ — размер файла скриншота $> 1$ КБ (исключен технический брак).
   * $C(u)$ — контраст текста и элементов управления соответствует WCAG 2.1 AA ($\ge 4.5:1$).
   * $M(u)$ — информационная плотность экрана не превышает лимиты Закона Миллера ($\le 9$ независимых групп).

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(u) \iff O (S(u) \land R(u) \land Q(u) \land C(u) \land M(u))$$
     Роль обязана согласовать макет при деонтическом требовании полноты разрешений, адаптивности, корректности рендеринга, высокой контрастности и эргономичности.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(u) \iff \lozenge (\neg S(u) \lor \neg R(u) \lor \neg Q(u) \lor \neg C(u) \lor \neg M(u))$$
     Роль блокирует макет при обнаружении малейшей вероятности нарушения доступности, отсутствия адаптивной верстки или превышения когнитивных лимитов восприятия.

3. **Функция Ценности (Value Function) и веса метрик:**
   Максимальный фокус на пользовательской ценности и перцептивном качестве дизайна при минимизации лишней когнитивной сложности.
   $$Evaluation = 0.15 \cdot T + 0.35 \cdot Q + 0.40 \cdot V - 0.10 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.15 — скорость проектирования важна для непрерывности продуктового цикла.
   * $w_2$ (Качество $Q$) = 0.35 — строгое соблюдение эргономических стандартов, законов Гештальта и Фиттса.
   * $w_3$ (Ценность $V$) = 0.40 — наивысшая полезность, интуитивность и удобство достижения целей пользователем.
   * $w_4$ (Сложность $C$) = 0.10 — минимизация визуального мусора и усложнения интерфейсных переходов.

4. **Ключевая проектная метрика (KPI роли):**
   *Индекс когнитивного удобства (Cognitive Usability Index / CUI)* — процент макетов, успешно прошедших проверку DesignExcellenceGate с первого предъявления без отклонений по критериям контраста, адаптивности и информационной плотности.
""",
    "BARCAN-TAG-04_MODAL-QUANTIFIER.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Вероятностная эпистемическая логика (эпистемическая модель знания с байесовским обновлением вероятностей). Описывает степень обоснованности предсказаний ИИ и верность логических выводов на основе строгих эмпирических свидетельств.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $m$ — модель машинного обучения или ИИ-пайплайн.
   Определим предикаты:
   * $G(m)$ — метрика Groundedness (обоснованность ответов в источнике) $\ge 0.85$.
   * $C(m)$ — Chain-of-Thought (Coherence) логически связен и верифицирован.
   * $L(m)$ — происхождение данных (Data Lineage) подтверждено и сертифицировано TAG-08.
   * $D(m)$ — уровень Data Drift (дрейфа данных) находится в допустимых пределах.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(m) \iff \Box (G(m) \land C(m) \land L(m) \land \neg D(m))$$
     Роль одобряет внедрение модели только тогда, когда обоснованность ответов, логическая строгость, чистота данных и стабильность распределения выполняются с абсолютной необходимостью.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(m) \iff \lozenge (\neg G(m) \lor \neg C(m) \lor \neg L(m) \lor D(m))$$
     Роль блокирует деплой, если существует возможность получения необоснованных ответов (галлюцинаций), нарушения логики вывода, грязных данных или дрейфа распределения.

3. **Функция Ценности (Value Function) и веса метрик:**
   Роль балансирует между предсказательной ценностью ИИ-решений и качеством валидации для предотвращения галлюцинаций.
   $$Evaluation = 0.10 \cdot T + 0.40 \cdot Q + 0.35 \cdot V - 0.15 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.10 — обучение и валидация моделей требуют детерминированного времени для сбора логов и holdout-тестирования.
   * $w_2$ (Качество $Q$) = 0.40 — максимальная точность предсказаний, строгость Coherence и отсутствие ложных выводов.
   * $w_3$ (Ценность $V$) = 0.35 — непосредственная бизнес-полезность ИИ-сервисов в оптимизации процессов.
   * $w_4$ (Сложность $C$) = 0.15 — минимизация стоимости инференса, вычислительного техдолга и сложности пайплайнов.

4. **Ключевая проектная метрика (KPI роли):**
   *Индекс заземленности ИИ (AI Groundedness & Consistency Score / AGCS)* — процент ответов интеллектуальных агентов и моделей в продакшене, успешно прошедших детерминированную автоматическую проверку на отсутствие галлюцинаций и логических противоречий.
""",
    "BARCAN-TAG-05_NECESSARY-IDENTITY.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Каузальная алетическая логика (логика физической и системной необходимости). Моделирует причинно-следственные связи инфраструктурных инцидентов, обеспечивая предсказуемость состояния систем через воспроизводимость конфигураций (IaC).

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $s$ — состояние инфраструктурной среды.
   Определим предикаты:
   * $IaC(s)$ — вся конфигурация серверов и сетей полностью определена в виде кода без ручных правок.
   * $SLO(s)$ — доступность и задержка (latency) находятся строго в пределах лимитов SLO.
   * $R(s)$ — для изменений $s$ разработан и протестирован автоматический скрипт отката (Rollback Strategy).
   * $M(s)$ — миграции БД выполнены последовательно через Flyway с использованием безопасных типов данных.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(s) \iff \Box (IaC(s) \land SLO(s) \land R(s) \land M(s))$$
     Роль одобряет инфраструктурное изменение только при необходимости обеспечения воспроизводимости, соблюдения SLO, обратимости изменений и строгости миграций.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(s) \iff \lozenge (\neg IaC(s) \lor \neg SLO(s) \lor \neg R(s) \lor \neg M(s))$$
     Роль блокирует релиз, если возможен дрейф конфигурации из-за ручных правок, нарушение бюджета ошибок, отсутствие четкого отката или хаос в миграциях.

3. **Функция Ценности (Value Function) и веса метрик:**
   Роль фокусируется на надежности и безопасности инфраструктурных сред при минимизации сложности конфигураций.
   $$Evaluation = 0.15 \cdot T + 0.45 \cdot Q + 0.15 \cdot V - 0.25 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.15 — автоматизация развертывания ускоряет CI/CD пайплайны.
   * $w_2$ (Качество $Q$) = 0.45 — обеспечение непрерывной доступности, отказоустойчивости и каузальной предсказуемости среды.
   * $w_3$ (Ценность $V$) = 0.15 — стабильность окружения обеспечивает базовую ценность функционирования всего бизнеса.
   * $w_4$ (Сложность $C$) = 0.25 — борьба с дрейфом конфигураций, избыточными сервисами и неявными связями.

4. **Ключевая проектная метрика (KPI роли):**
   *Коэффициент каузальной стабильности (Causal Infrastructure Stability Index / CISI)* — среднее время восстановления работоспособности (MTTR) при возникновении инцидентов, устраненных исключительно средствами автоматического наката/отката IaC.
""",
    "BARCAN-TAG-06_DEONTIC-CONSISTENCY.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Деонтическая логика согласованности и обязательств (логика деонтических систем фон Вригта). Формализует деонтические статусы (обязательно, разрешено, запрещено) в управлении качеством программного обеспечения, гарантируя эмпирическую верифицируемость кода.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $t$ — тестовое покрытие и качество фичи.
   Определим предикаты:
   * $P(t)$ — пирамида тестирования строго соблюдена (соотношение юнит/интеграционных/E2E тестов).
   * $Cov(t)$ — покрытие кода автотестами выше установленных порогов ($\ge 85\%$ для ядра, $\ge 60\%$ для периферии).
   * $F(t)$ — уровень Flaky-тестов в пайплайне равен нулю (все тесты детерминированы).
   * $AC(t)$ — требования фичи декомпозированы на верифицируемые Acceptance Criteria в формате Given-When-Then.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(t) \iff O (P(t) \land Cov(t) \land F(t) \land AC(t))$$
     Роль обязана пропустить фичу в продакшен только при обязательном соблюдении стандартов покрытия, детерминированности автотестов и формализации критериев приемки.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(t) \iff P (\neg P(t) \lor \neg Cov(t) \lor \neg F(t) \lor \neg AC(t))$$
     Роль обязана атаковать и заблокировать мерж, если разрешено (наблюдается) отклонение от пирамиды тестов, снижение покрытия, появление нестабильных (flaky) тестов или неопределенность требований.

3. **Функция Ценности (Value Function) и веса метрик:**
   Бескомпромиссная ориентация на качество покрытия и снижение технической и тестовой сложности системы.
   $$Evaluation = 0.15 \cdot T + 0.50 \cdot Q + 0.10 \cdot V - 0.25 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.15 — высокая скорость выполнения автотестов в CI критически важна для продуктового потока.
   * $w_2$ (Качество $Q$) = 0.50 — максимальный приоритет отдается надежности, корректности логики и отсутствию регрессий.
   * $w_3$ (Ценность $V$) = 0.10 — бизнес-ценность должна быть подтверждена проверяемыми тестами, а не декларативными заявлениями.
   * $w_4$ (Сложность $C$) = 0.25 — штраф за раздутые, хрупкие тесты, сложную логику мокирования и избыточный код.

4. **Ключевая проектная метрика (KPI роли):**
   *Индекс эмпирической верифицируемости (Empirical Verifiability Index / EVI)* — процентная доля успешно пройденных, стабильных автотестов в CI без ложноположительных или ложноотрицательных срабатываний (целевое значение: 100%).
""",
    "BARCAN-TAG-07_SECOND-ORDER-KNOWLEDGE.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Эпистемическая логика безопасности (модель Zero-Trust с приматом знания). Описывает безопасность как доказанный факт владения криптографическим знанием без возможности несанкционированного вывода конфиденциальной информации.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $a$ — реализуемая функциональность или Pull Request.
   Определим предикаты:
   * $V(a)$ — статический и динамический анализ (SAST/DAST) подтверждают отсутствие уязвимостей.
   * $Sec(a)$ — все секреты и PII полностью изолированы и хранятся исключительно в Vault, исключая хардкод.
   * $T(a)$ — угрозы безопасности смоделированы согласно STRIDE совместно с TAG-01.
   * $Auth(a)$ — в коде реализована строгая верификация прав доступа и валидация входящих параметров.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(a) \iff \Box (V(a) \land Sec(a) \land T(a) \land Auth(a))$$
     Роль согласовывает проект только при жесткой необходимости полного прохождения аудита безопасности, изоляции секретов, моделирования угроз и валидации прав.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(a) \iff \lozenge (\neg V(a) \lor \neg Sec(a) \lor \neg T(a) \lor \neg Auth(a))$$
     Роль обязана заблокировать мерж, если существует малейшая возможность утечки секретов, наличия уязвимостей в коде, неполноты логов авторизации или пропуска валидации ввода.

3. **Функция Ценности (Value Function) и веса метрик:**
   Роль ориентирована на исключение рисков компрометации данных и минимизацию сложности уязвимых компонентов.
   $$Evaluation = 0.10 \cdot T + 0.50 \cdot Q + 0.10 \cdot V - 0.30 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.10 — аудит безопасности требует времени, скорость релиза не может ставиться выше защиты системы.
   * $w_2$ (Качество $Q$) = 0.50 — бескомпромиссная защищенность, целостность и конфиденциальность архитектуры.
   * $w_3$ (Ценность $V$) = 0.10 — бизнес-ценность уничтожается при первом же инциденте безопасности.
   * $w_4$ (Сложность $C$) = 0.30 — строгий контроль над усложнением векторов атак, избыточными привилегиями и правами доступа.

4. **Ключевая проектная метрика (KPI роли):**
   *Индекс защищенности нулевого доверия (Zero-Trust Security Index / ZTSI)* — процент закрытых векторов атак STRIDE на стадии статического анализа и полное отсутствие критических уязвимостей (CVSS $\ge 7.0$) в исходном коде.
""",
    "BARCAN-TAG-08_SUBSTITUTIVITY-SALVA-VERITATE.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Темпоральная интуиционистская логика типов (применение теории типов Рассела и Мартин-Лёфа к реляционным и аналитическим структурам данных). Гарантирует сохранение истинности и структуры данных при любых трансформациях и миграциях во времени.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $m$ — новая СУБД-миграция или ETL-пайплайн.
   Определим предикаты:
   * $Seq(m)$ — миграция имеет строго уникальный, последовательный номер Flyway без коллизий и пропусков.
   * $C(m)$ — состояние данных в Data Catalog стабильно, согласовано (Consistent) и нормализовано.
   * $R(m)$ — миграция имеет проверенный автоматический сценарий отката (down migration).
   * $A(m)$ — аналитические ресурсоемкие операции изолированы от основного транзакционного OLTP-узла.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(m) \iff \Box (Seq(m) \land C(m) \land R(m) \land A(m))$$
     Роль одобряет миграцию только тогда, когда строгость нумерации, согласованность схемы, наличие отката и изоляция OLAP выполняются с абсолютной необходимостью.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(m) \iff \lozenge (\neg Seq(m) \lor \neg C(m) \lor \neg R(m) \lor \neg A(m))$$
     Роль заявляет отказ (Refusal) and блокирует деплой при малейшей вероятности коллизии версий миграций, нарушения целостности схемы, отсутствия отката или нагрузки на OLTP-мастер.

3. **Функция Ценности (Value Function) и веса метрик:**
   Роль направлена на обеспечение идеальной целостности хранилищ и минимизацию сложности схем.
   $$Evaluation = 0.10 \cdot T + 0.45 \cdot Q + 0.15 \cdot V - 0.30 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.10 — изменения структуры данных фундаментальны и требуют строгого планирования.
   * $w_2$ (Качество $Q$) = 0.45 — безупречная нормализация, согласованность типов и точность ETL-преобразований.
   * $w_3$ (Ценность $V$) = 0.15 — ценность данных определяется их доступностью и достоверностью для аналитики и ИИ.
   * $w_4$ (Сложность $C$) = 0.30 — высокий штраф за избыточную денормализацию, коллизии миграций и появление неиспользуемых полей.

4. **Ключевая проектная метрика (KPI роли):**
   *Индекс целостности схем данных (Database Schema Integrity Index / DSII)* — процент миграций Flyway, успешно развернутых на всех средах без конфликтов версий, ошибок согласованности (FK/Check) и без необходимости ручного вмешательства DBA.
""",
    "BARCAN-TAG-09_MORAL-DILEMMA.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Деонтическая прагматическая логика принятия решений (формализация деонтических обязательств технического лидера перед целостностью всей системы). Обеспечивает защиту системы от бесконтрольной самогенерации задач и накопления Lean-потерь.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $t$ — предлагаемая к реализации задача (из Wishlist).
   Определим предикаты:
   * $J(t)$ — задача обладает четко сформулированным JTBD, описывающим измеримое изменение состояния системы.
   * $L(t)$ — Lean-ценность задачи подтверждена (не является категорией `waste`).
   * $C(t)$ — задача явно ссылается на расширение текущего системного ограничения по TOC ($toc\_constraint\_ref$).
   * $S(t)$ — определена количественная метрика успеха в парадигме Six Sigma ($six\_sigma\_metric$).
   * $G(t)$ — критерии приемки ($acceptance\_criteria$) детерминированы по паттерну Given-When-Then.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(t) \iff O (J(t) \land L(t) \land C(t) \land S(t) \land G(t))$$
     Роль обязана скомпилировать идею в задачу только при обязательном соблюдении измеримости JTBD, отсутствии Lean-потерь, соответствии TOC-ограничению и строгости критериев.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(t) \iff P (\neg J(t) \lor \neg L(t) \lor \neg C(t) \lor \neg S(t) \lor \neg G(t))$$
     Роль обязана жестко отклонить (Reject) пожелание, если разрешено (наблюдается) отсутствие измеримой метрики, классификация задачи как `waste`, локальная оптимизация вне бутылочного горлышка или неполнота критериев приемки.

3. **Функция Ценности (Value Function) и веса метрик:**
   Максимальная ориентация на доставку системной ценности и скорость расширения ограничений, жестко ограничивая нарастание хаоса.
   $$Evaluation = 0.25 \cdot T + 0.20 \cdot Q + 0.45 \cdot V - 0.10 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.25 — скорость декомпозиции и продвижения задач по TOC-цепочке является критичной.
   * $w_2$ (Качество $Q$) = 0.20 — строгость спецификаций и отсутствие двусмысленности в требованиях.
   * $w_3$ (Ценность $V$) = 0.45 — наивысший приоритет отдается максимизации системной ценности и ликвидации потерь (Muda).
   * $w_4$ (Сложность $C$) = 0.10 — контроль над размером WIP и предотвращение избыточной декомпозиции.

4. **Ключевая проектная метрика (KPI роли):**
   *Коэффициент прагматической плотности бэклога (Backlog Value-to-Waste Ratio / BVWR)* — отношение количества задач, успешно прошедших Lean/TOC-фильтр и дошедших до релиза без изменений требований, к общему числу заявок в Wishlist.
""",
    "BARCAN-TAG-10_DEONTIC-PROHIBITION.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Деонтическая логика запретов и нормативного соответствия (формализация нормативных запретов GDPR/152-ФЗ). Фокусируется на обеспечении безопасности персональных данных и защите прав субъектов через принудительное маскирование и шифрование.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $d$ — функционал обработки персональных данных или изменение схемы хранения.
   Определим предикаты:
   * $PIA(d)$ — проведение оценки воздействия на конфиденциальность (Privacy Impact Assessment) успешно завершено и одобрено.
   * $RoPA(d)$ — запись о деятельности по обработке (Record of Processing Activities) обновлена и зафиксирована.
   * $L(d)$ — соблюдены требования к локализации данных и получено верифицированное юридическое согласие субъекта.
   * $M(d)$ — в логах и стейджинг-средах принудительно применяется маскирование и шифрование PII.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(d) \iff O (PIA(d) \land RoPA(d) \land L(d) \land M(d))$$
     Роль обязана согласовать изменения только при деонтическом требовании прохождения PIA, актуальности RoPA, юридической чистоты локализации и маскирования.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(d) \iff \lozenge (\neg PIA(d) \lor \neg RoPA(d) \lor \neg L(d) \lor \neg M(d))$$
     Роль обязана заблокировать релиз при малейшей возможности сбора персональных данных без согласия, нарушения локализации, ослабления защиты в целях отладки или отсутствия сроков хранения (Retention Policy).

3. **Функция Ценности (Value Function) и веса метрик:**
   Роль ставит юридическую безопасность и минимизацию регуляторных рисков выше скорости разработки.
   $$Evaluation = 0.10 \cdot T + 0.45 \cdot Q + 0.15 \cdot V - 0.30 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.10 — проведение комплаенс-проверок и PIA требует детерминированного времени.
   * $w_2$ (Качество $Q$) = 0.45 — абсолютное соответствие законодательным нормам, исключение утечек PII.
   * $w_3$ (Ценность $V$) = 0.15 — доверие пользователей и репутация компании формируют долгосрочную ценность.
   * $w_4$ (Сложность $C$) = 0.30 — высокий штраф за избыточный сбор данных, усложнение схем согласий и накопление неиспользуемых PII.

4. **Ключевая проектная метрика (KPI роли):**
   *Индекс нормативного соответствия (Regulatory Compliance Index / RCI)* — процент процессов обработки персональных данных, полностью отраженных в RoPA и прошедших автоматизированный аудит безопасности на предмет отсутствия PII в открытом виде (целевое значение: 100%).
""",
    "BARCAN-TAG-11_CLIENT-PERCEPTION.md": r"""

### Математический и логический аппарат роли

1. **Базовый модально-логический базис:**
   Интенсиональная логика пространственно-временного интерфейса (формализация временных задержек и визуальной стабильности рендеринга). Обеспечивает мгновенность отклика и стабильность визуальной картины мира пользователя в рамках когнитивных циклов мозга.

2. **Предикаты принятия решений (Формулы согласия и атаки):**
   Пусть $f$ — фронтенд-реализация (компонент Svelte, страница).
   Определим предикаты:
   * $S(f)$ — предоставлены скриншоты реализации для двух обязательных разрешений (1440px и 375px).
   * $R(f)$ — верстка адаптивна, размеры скриншотов по байтам не совпадают (исключена фиктивная адаптивность).
   * $C(f)$ — показатели стабильности верстки CLS $< 0.1$ и скорости загрузки LCP $\le 2.5s$ полностью соблюдены.
   * $A(f)$ — используются семантические теги HTML5 и ARIA-атрибуты для обеспечения программной доступности.

   * **Формула Согласия (Acceptance Criteria):**
     $$Accept(f) \iff O (S(f) \land R(f) \land C(f) \land A(f))$$
     Роль обязана принять реализацию только при обязательном наличии скриншотов, адаптивности верстки, соблюдении Core Web Vitals и доступности по стандартам WCAG.

   * **Формула Атаки (Critique Trigger):**
     $$Attack(f) \iff \lozenge (\neg S(f) \lor \neg R(f) \lor \neg C(f) \lor \neg A(f))$$
     Роль отвергает реализацию, если существует возможность сдвига контента (CLS $\ge 0.1$), медленной загрузки, отсутствия семантики или отсутствия адаптивного макета для мобильных тач-экранов.

3. **Функция Ценности (Value Function) и веса метрик:**
   Наивысший приоритет отдается качеству перцептивного восприятия и пользовательской ценности фронтенд-приложения.
   $$Evaluation = 0.15 \cdot T + 0.35 \cdot Q + 0.40 \cdot V - 0.10 \cdot C$$
   Весовые коэффициенты:
   * $w_1$ (Скорость / Time to Market $T$) = 0.15 — быстрая поставка UI важна для непрерывного получения обратной связи.
   * $w_2$ (Качество $Q$) = 0.35 — строгое соответствие показателям Core Web Vitals и стандартам инклюзивной верстки.
   * $w_3$ (Ценность $V$) = 0.40 — обеспечение беспрепятственного, отзывчивого и интуитивного взаимодействия пользователя с системой.
   * $w_4$ (Сложность $C$) = 0.10 — минимизация размера бандла, избыточного JS-кода и перегруженных компонентов DOM.

4. **Ключевая проектная метрика (KPI роли):**
   *Индекс перцептивного качества фронтенда (Frontend Perceptual Performance Index / FPPI)* — интегральный показатель, вычисляемый на основе соответствия Core Web Vitals (CLS, LCP, FID) и 100% прохождения автоматических тестов доступности Axe Core в CI.
"""
}

def main():
    print("Starting append operations...")
    for filename, text in ROLE_LOGIC.items():
        filepath = os.path.join(".", filename)
        if not os.path.exists(filepath):
            print(f"Error: File {filepath} does not exist.")
            continue

        # Read the file
        with open(filepath, "r", encoding="utf-8") as f:
            content = f.read()

        # Check if already appended to ensure idempotence
        if "### Математический и логический аппарат роли" in content:
            print(f"File {filename} already contains the target section. Skipping.")
            continue

        # Ensure it ends with a newline
        if not content.endswith("\n"):
            content += "\n"

        # Append the new text block
        new_content = content + text

        # Write back
        with open(filepath, "w", encoding="utf-8") as f:
            f.write(new_content)

        print(f"Successfully appended logic block to {filename}")

if __name__ == "__main__":
    main()
